#!/usr/bin/env -S bash -i
# shellcheck shell=bash

# __bash_bin=$;
# builtin exec "${__bash_bin}" --login;
# source "${HOME}/.profile";
# source "${HOME}/.bashrc";
# unset __bash_bin;

# set -euxo pipefail;

open -a "/Applications/RStudio.app";
